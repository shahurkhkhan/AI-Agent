name: PR Review Agent
description: AI PR review for Angular/NX

inputs:
  github-token:
    required: true
    default: ${{ github.token }}

  openai-api-key:
    required: true

runs:
  using: node20
  main: dist/apps/pr-review-agent-runner/main.js