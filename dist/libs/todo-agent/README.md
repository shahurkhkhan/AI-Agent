# todo-agent

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build todo-agent` to build the library.

## Running unit tests

Run `nx test todo-agent` to execute the unit tests via [Jest](https://jestjs.io).
