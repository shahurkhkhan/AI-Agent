# ng-pr-review-agent

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ng-pr-review-agent` to build the library.

## Running unit tests

Run `nx test ng-pr-review-agent` to execute the unit tests via [Jest](https://jestjs.io).
